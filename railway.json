{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo 'DEBUG: PORT='$PORT && echo 'DEBUG: APP_URL='$APP_URL && php artisan migrate --force && php artisan config:cache && php artisan serve --host=0.0.0.0 --port=$PORT --verbose",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
